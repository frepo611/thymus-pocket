# 2026-05-28

## Vad gjordes idag

### POC-plan fastställd

Skapade och låste `docs/poc-plan-blazor-frontend.md` med en fyrastegsplan mot Blazor-frontend via BFF.
Beslut: Minimal API i POC-fasen med en tydlig beslutsregel för när Controllers ska utvärderas (ca 20–30 endpoints eller fler parallella team).

### SMF action-katalog

Extraherade den fullständiga action-katalogen direkt från `smf_2-0-19_install/index.php` (80 actions) och sparade den i `docs/smf/action-catalog.json`.
Lade till en `versionDiffs`-sektion som dokumenterar exakta skillnader mot SMF 2.1.0 för att undvika att agenter blandar ihop versionerna.
Kopplat som "source of truth" i `.github/copilot-instructions.md`.

### Fas 1 – Adapterbibliotek (klart)

Skapade `src/Thymus.SmfAdapter` med:

- `SmfHttpClient` – flyttad från console-projektet, renodlad (logik utan Console.WriteLine)
- `SmfHtmlParser` – extraherat som statisk parser-klass för trådlistor, board-URLs och topic-inlägg
- Modeller: `ThreadSummary`, `PostContent`, `AuthenticationArtifacts`

`Thymus.Poc` refaktorerades att referera adapterbiblioteket och sina gamla duplicerade filer togs bort.

### Fas 2 – Minimal API BFF (klart)

Skapade `src/Thymus.Bff` med:

- `POST /api/auth/login` — autentisering mot SMF, sätter HttpOnly session-cookie (`thymus_session`)
- `POST /api/auth/logout` — loggar ut och rensar sessionen
- `GET /api/threads` — returnerar trådlista som JSON
- `GET /api/threads/{id}` — returnerar tråd med inlägg
- `POST /api/threads/{id}/replies` — postar svar

Forum-cookies hanteras server-side i per-session cookie-filer. Swagger aktivt i Development.

### Parsertester (klart, 3/3 gröna)

Skapade `tests/Thymus.SmfAdapter.Tests` med tre parsertester:

- `ParseThreadList_ParsesClassicTableRows`
- `ParseBoardUrls_FiltersOutUnreadActionLinks`
- `ParseTopic_ParsesPostCards`

### Build

Hela solutionen bygger rent (0 warnings, 0 errors). Alla tester gröna.

### Fas 3 – Frontend scaffold + commit-struktur

Skapade en ren Blazor-scaffold för `src/Thymus.Web` utan implementerade features och committade separat:

- `e599f2c` — `chore: scaffold Thymus.Web utan features`

Därefter delades återstående arbete upp i separata commits för tydligare historik:

- `ef07c15` — `chore(bff): add development launch settings`
- `067af74` — `test(web): add BffApiClient integration tests`
- `394e28c` — `chore(solution): include web and web tests projects`

### Testanpassning efter scaffold

Efter scaffolden saknades tidigare feature-typer i `Thymus.Web`, vilket bröt webtesterna.
Integrationstesterna anpassades därför för att bli självförsörjande i testprojektet (testlokal klient/DTO) och committades separat:

- `9e556e4` — `test(web): decouple bff integration tests from web feature types`

Verifiering efter anpassning:

- Solution build: grönt
- `BffApiClientIntegrationTests`: 4/4 gröna

## Nästa steg

- Implementera Fas 3-funktionalitet i `src/Thymus.Web` (login, trådlista, trådvy och svar via BFF)
- Behålla commit-uppdelning per delområde (web, bff, tests, solution)
